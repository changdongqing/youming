# 详细设计-DD11-可视化建模画布

| 项 | 内容 |
|---|---|
| 文档名称 | 可视化建模画布 详细设计 |
| 里程碑 | M9（FR-17） |
| 上游 PRD | 《本体建模功能产品需求文档.md》v1.0（FR-17 / AC-17.1~17.8 / 9.7 / 10.7 / 十二节 12.6 / 十四节 M9） |
| 设计计划 | 《本体建模功能详细设计计划.md》DD11（M9） |
| 前置依赖 | DD7-DD10 全部落地（项目/类/属性/关系/序列化接口可调）；治理域 Supply API（class-template/tree + property-templates + units）可消费 |
| 编写日期 | 2026-07-28 |
| 文档状态 | 评审通过（附优化项已合入），进入实现 |

> **评审决策记录**（2026-07-28 评审合入）：
> - **getCurrentUserId 改为 SecurityUtils.getUser().getUsername()**（P-1）：原硬编码 "admin" 导致所有用户共享画布状态，违反 R-25。DD11 是 ontology-biz 首个需主动取用户的服务，用 `SecurityUtils.getUser().getUsername()` 取当前用户名（与 createBy 审计字段一致的 username 字符串），存入 `user_id` 字段（varchar(64)）。方法名改为 `getCurrentUsername`（P-3）。
> - **合并重复的 index.vue**（P-2）：前端文件清单原有两个 index.vue（L99+L111），合并为一个主入口。
> - **补充 mitt 事件键定义**（P-4）：在文档明确需新增的 MittType 键（canvasNodeAdded/canvasEdgeAdded/openPropertyDrawer/edgeConnected/refreshSerializePreview）。
> - **前端直调 Supply API 鉴权说明**（P-5）：前端 request 拦截器自动带 Authorization token，直调 `/admin/ont/supply/v1/` 无鉴权问题（与后端 RestTemplate 调后端不同）。

---

## 一、设计目标与范围

### 1.1 目标

- 提供 AntV X6 驱动的可视化建模画布，类为节点、subClassOf 为继承边、对象属性为关联边。
- 左侧模板侧边栏实时拉取治理域 Supply API，拖拽分类模板到画布即可实例化为类节点（调 DD8 接口）。
- 双击节点弹出右侧抽屉编辑属性（调 DD9 接口）；端口拖出建关系（subClassOf / 对象属性）。
- 底部可折叠序列化预览面板（调 DD10 preview 接口 + CodeEditor 展示 Turtle）。
- 画布状态（节点位置/缩放）持久化到 `ont_model_canvas_state` 表，每用户独立。
- V16 建表 + 11500 段菜单种子。
- 新增前端依赖 `@antv/x6` + `@antv/x6-vue-shape` + `@antv/layout`。

### 1.2 范围（本 DD 做 / 不做）

| 做（本 DD） | 不做（后续 DD / Out of Scope） |
|---|---|
| 画布渲染（类节点+继承边+关联边）（FR-17.1） | 多用户协同编辑（Out of Scope，PRD 第十八节） |
| 模板侧边栏（三 Tab 拉取 Supply）（FR-17.2） | 本体版本对比与合并（Out of Scope） |
| 拖拽创建（分类模板->类节点实例化）（FR-17.3） | SPARQL 查询终端（Out of Scope） |
| 节点编辑（双击->属性抽屉）（FR-17.4） | 个体实例画布（M10/DD12，Out of Scope） |
| 边创建（端口拖出->关系选择）（FR-17.5） | |
| 序列化预览面板（防抖 500ms）（FR-17.6） | |
| 画布布局（自动+手动拖拽）（FR-17.7） | |
| 画布状态持久化（FR-17.8） | |
| V16 建表 + 11500 菜单种子 | |

### 1.3 验收映射（M9 DoD）

| PRD AC | 本 DD 实现点 |
|---|---|
| AC-17.1 画布渲染类节点+继承边+关联边 | 4.2 CanvasCore（X6 Graph 初始化 + 自定义节点/边） |
| AC-17.2 模板侧边栏三 Tab 拉取 Supply | 5.2 TemplateSidebar.vue（调 supplyClassTemplateTree + supply property-templates + supply units） |
| AC-17.3 拖拽分类模板实例化为类节点 | 5.3 拖拽处理（dnd -> 调 class.addObj 含 templateCode -> 画布渲染节点） |
| AC-17.4 双击节点编辑属性 | 5.4 PropertyDrawer.vue（双击 node:dblclick -> 调 class.getObj + dt/obj prop 接口） |
| AC-17.5 端口拖出建关系 | 5.5 边创建处理（edge:connected -> 弹关系选择 -> 调 subclassof.addEdge 或 object-property.addObj） |
| AC-17.6 序列化预览面板防抖 500ms | 5.6 SerializePreviewPanel.vue（防抖 500ms -> 调 serialize.preview -> CodeEditor 展示） |
| AC-17.7 自动布局+手动拖拽 | 4.2 CanvasCore（dagre 布局 + panning/zooming） |
| AC-17.8 画布状态持久化 | 4.3 ModelCanvasController + 4.4 ServiceImpl（graph.toJSON -> ont_model_canvas_state） |

---

## 二、落地清单

### 2.1 后端文件清单

```
server/pig-ontology/pig-ontology-biz/src/main/java/com/pig4cloud/pig/ontology/modeling/
├── controller/
│   └── ModelCanvasController.java            # 新增：画布状态 CRUD + 图数据查询
├── service/
│   ├── ModelCanvasService.java               # 新增
│   └── impl/
│       └── ModelCanvasServiceImpl.java       # 新增
├── mapper/
│   └── ModelCanvasStateMapper.java           # 新增
└── entity/
    └── ModelCanvasState.java                 # 新增
```

### 2.2 数据库文件清单

```
server/pig-common/pig-common-data/src/main/resources/db/migration/
└── V16__ont_model_canvas_seed.sql            # 新增：建 ont_model_canvas_state + 11500 菜单
```

### 2.3 依赖变更清单

新增前端依赖（`web/package.json`）：
```json
"@antv/x6": "^2.18.1",
"@antv/x6-vue-shape": "^2.1.2",
"@antv/layout": "^1.2.6"
```

无新增后端依赖。

### 2.4 前端文件清单

```
web/src/
├── api/ontology-model/
│   └── canvas.ts                             # 新增：画布状态 API
└── views/admin/ontology-model/canvas/
    ├── index.vue                             # 新增：画布主页面（三栏布局，主入口）
    ├── components/
    │   ├── TemplateSidebar.vue               # 新增：左侧模板侧边栏
    │   ├── ClassNode.vue                     # 新增：X6 自定义节点组件
    │   ├── PropertyDrawer.vue                # 新增：右侧属性编辑抽屉
    │   └── SerializePreviewPanel.vue         # 新增：底部序列化预览
    ├── composables/
    │   ├── useCanvas.ts                      # 新增：画布核心逻辑（初始化/拖拽/布局/撤销重做）
    │   └── useCanvasDnd.ts                   # 新增：拖拽创建逻辑
    └── i18n/
        ├── zh-cn.ts                          # 新增
        └── en.ts                             # 新增
```

---

## 三、数据库设计（V16）

### 3.1 V16 脚本范围

V16 一次性完成：
- (a) 建表 `ont_model_canvas_state`（画布状态持久化）
- (b) 建索引
- (c) sys_menu 菜单种子（11500 段"可视化画布" + 11501~11502 权限点按钮）

### 3.2 V16__ont_model_canvas_seed.sql

```sql
-- ============================================================
-- V16: 建模域 - 可视化画布状态持久化（FR-17）
-- 建 ont_model_canvas_state + 菜单种子（11500 段）
-- ============================================================

-- ---------- (a) 建表 ----------

CREATE TABLE ont_model_canvas_state (
    id              bigint       NOT NULL,
    project_id      bigint       NOT NULL,
    user_id         varchar(64)  NOT NULL,
    graph_data      text,
    create_by       varchar(64)  DEFAULT ' ',
    create_time     timestamp    DEFAULT now(),
    update_by       varchar(64)  DEFAULT ' ',
    update_time     timestamp    DEFAULT now(),
    del_flag        char(1)      DEFAULT '0',
    CONSTRAINT pk_ont_model_canvas_state PRIMARY KEY (id),
    CONSTRAINT uk_ont_model_canvas_state UNIQUE (project_id, user_id)
);
COMMENT ON TABLE  ont_model_canvas_state IS '可视化画布状态持久化（每项目每用户，FR-17.8）';
COMMENT ON COLUMN ont_model_canvas_state.project_id IS '所属项目 ID';
COMMENT ON COLUMN ont_model_canvas_state.user_id    IS '用户 ID（每用户独立画布状态）';
COMMENT ON COLUMN ont_model_canvas_state.graph_data IS 'AntV X6 graph.toJSON() 序列化的图数据（节点位置/边/缩放）';

-- ---------- (b) 索引 ----------

CREATE INDEX idx_ont_model_canvas_project ON ont_model_canvas_state (project_id) WHERE del_flag = '0';

-- ---------- (c) sys_menu 菜单种子（11500 段） ----------

INSERT INTO sys_menu VALUES (11500, '可视化画布', NULL, '/admin/ontology-model/canvas/index', NULL, 11000, 'iconfont icon-tubiao', '1', 5, '0', '0', '0', 'admin', now(), 'admin', now(), '0');
INSERT INTO sys_menu VALUES (11501, '画布查看', 'ont_canvas_view',   NULL, NULL, 11500, NULL, '1', 1, '0', NULL, '1', 'admin', now(), 'admin', now(), '0');
INSERT INTO sys_menu VALUES (11502, '画布编辑', 'ont_canvas_manage', NULL, NULL, 11500, NULL, '1', 2, '0', NULL, '1', 'admin', now(), 'admin', now(), '0');
```

> **sys_menu 17 字段**：按位置 INSERT，parent_id=11000（本体建模目录），菜单 menu_type='0'，按钮 menu_type='1'。

> **权限标识**：`ont_canvas_view`（画布查看/加载）、`ont_canvas_manage`（画布编辑/保存状态）。画布内的建模操作（建类/属性/关系）复用 DD8/DD9 的权限点（`ont_class_model_manage` / `ont_prop_model_manage`）。

---

## 四、后端设计

> 包路径 `com.pig4cloud.pig.ontology.modeling.*`。Controller 路径 `/ont/model/canvas`，对外 `/admin/ont/model/canvas/**`。

### 4.1 Entity - ModelCanvasState

```java
package com.pig4cloud.pig.ontology.modeling.entity;

import com.baomidou.mybatisplus.annotation.*;
import com.baomidou.mybatisplus.extension.activerecord.Model;
import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import lombok.EqualsAndHashCode;

import java.time.LocalDateTime;

/**
 * 画布状态持久化 Entity（FR-17.8）
 * <p>
 * 每项目每用户独立，存储 AntV X6 graph.toJSON() 序列化的图数据。
 *
 * @author pig
 * @date 2026-07-28
 */
@Data
@Schema(description = "画布状态")
@EqualsAndHashCode(callSuper = true)
@TableName("ont_model_canvas_state")
public class ModelCanvasState extends Model<ModelCanvasState> {

	private static final long serialVersionUID = 1L;

	@TableId(type = IdType.ASSIGN_ID)
	@Schema(description = "主键")
	private Long id;

	@Schema(description = "所属项目 ID")
	private Long projectId;

	@Schema(description = "用户 ID")
	private String userId;

	@Schema(description = "AntV X6 graph.toJSON() 序列化的图数据")
	private String graphData;

	@TableField(fill = FieldFill.INSERT)
	@Schema(description = "创建人")
	private String createBy;

	@TableField(fill = FieldFill.INSERT)
	@Schema(description = "创建时间")
	private LocalDateTime createTime;

	@TableField(fill = FieldFill.UPDATE)
	@Schema(description = "修改人")
	private String updateBy;

	@TableField(fill = FieldFill.UPDATE)
	@Schema(description = "修改时间")
	private LocalDateTime updateTime;

	@TableLogic
	@TableField(fill = FieldFill.INSERT)
	@Schema(description = "删除标记,1:已删除,0:正常")
	private String delFlag;
}
```

### 4.2 Controller - ModelCanvasController

```java
package com.pig4cloud.pig.ontology.modeling.controller;

import com.pig4cloud.pig.common.core.util.R;
import com.pig4cloud.pig.common.log.annotation.SysLog;
import com.pig4cloud.pig.common.security.annotation.HasPermission;
import com.pig4cloud.pig.ontology.modeling.entity.ModelCanvasState;
import com.pig4cloud.pig.ontology.modeling.service.ModelCanvasService;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.security.SecurityRequirement;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.AllArgsConstructor;
import org.springframework.http.HttpHeaders;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Map;

/**
 * 可视化画布 Controller（FR-17）
 * <p>
 * 路径 /ont/model/canvas/**，对外 /admin/ont/model/canvas/**
 *
 * @author pig
 * @date 2026-07-28
 */
@RestController
@AllArgsConstructor
@RequestMapping("/ont/model/canvas")
@Tag(name = "可视化画布", description = "画布状态持久化 + 图数据查询（FR-17）")
@SecurityRequirement(name = HttpHeaders.AUTHORIZATION)
public class ModelCanvasController {

	private final ModelCanvasService modelCanvasService;

	@GetMapping("/{projectId}/state")
	@Operation(summary = "获取画布状态", description = "当前用户的画布状态（AC-17.8）")
	@HasPermission("ont_canvas_view")
	public R<ModelCanvasState> getState(@PathVariable Long projectId) {
		return R.ok(modelCanvasService.getState(projectId));
	}

	@SysLog("保存画布状态")
	@PutMapping("/{projectId}/state")
	@Operation(summary = "保存画布状态", description = "graph.toJSON() 持久化（AC-17.8）")
	@HasPermission("ont_canvas_manage")
	public R saveState(@PathVariable Long projectId, @RequestBody ModelCanvasState state) {
		state.setProjectId(projectId);
		return modelCanvasService.saveState(state);
	}

	@GetMapping("/{projectId}/graph")
	@Operation(summary = "获取画布图数据", description = "类节点 + subClassOf 边 + 对象属性边（AC-17.1）")
	@HasPermission("ont_canvas_view")
	public R<Map<String, Object>> getGraph(@PathVariable Long projectId) {
		return R.ok(modelCanvasService.buildGraphData(projectId));
	}
}
```

> **getGraph 端点**：聚合项目下所有类（节点）+ subClassOf（继承边）+ 对象属性（关联边），返回 X6 格式的 nodes/edges JSON，供前端首次加载画布渲染。

### 4.3 ServiceImpl - ModelCanvasServiceImpl

```java
package com.pig4cloud.pig.ontology.modeling.service.impl;

import cn.hutool.core.util.StrUtil;
import com.baomidou.mybatisplus.core.toolkit.Wrappers;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.pig4cloud.pig.common.core.util.R;
import com.pig4cloud.pig.common.security.util.SecurityUtils;
import com.pig4cloud.pig.ontology.modeling.entity.*;
import com.pig4cloud.pig.ontology.modeling.mapper.*;
import com.pig4cloud.pig.ontology.modeling.service.ModelCanvasService;
import lombok.AllArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.*;

/**
 * 画布状态 Service 实现（FR-17.8）
 *
 * @author pig
 * @date 2026-07-28
 */
@AllArgsConstructor
@Service
public class ModelCanvasServiceImpl extends ServiceImpl<ModelCanvasStateMapper, ModelCanvasState>
		implements ModelCanvasService {

	private final ModelClassMapper modelClassMapper;
	private final ModelSubclassOfMapper subclassOfMapper;
	private final ModelObjectPropertyMapper objectPropertyMapper;

	@Override
	public ModelCanvasState getState(Long projectId) {
		// 当前用户名（与 createBy 审计字段一致，P-1 评审修正）
		String username = getCurrentUsername();
		return getOne(Wrappers.<ModelCanvasState>lambdaQuery()
			.eq(ModelCanvasState::getProjectId, projectId)
			.eq(ModelCanvasState::getUserId, username));
	}

	@Override
	@Transactional(rollbackFor = Exception.class)
	public R saveState(ModelCanvasState state) {
		String username = getCurrentUsername();
		state.setUserId(username);
		ModelCanvasState existing = getOne(Wrappers.<ModelCanvasState>lambdaQuery()
			.eq(ModelCanvasState::getProjectId, state.getProjectId())
			.eq(ModelCanvasState::getUserId, username));
		if (existing != null) {
			existing.setGraphData(state.getGraphData());
			return R.ok(updateById(existing));
		}
		return R.ok(save(state));
	}

	/**
	 * 构建画布图数据（AC-17.1）：类节点 + subClassOf 继承边 + 对象属性关联边
	 */
	@Override
	public Map<String, Object> buildGraphData(Long projectId) {
		// 查所有类
		List<ModelClass> classes = modelClassMapper.selectList(
				Wrappers.<ModelClass>lambdaQuery().eq(ModelClass::getProjectId, projectId));
		Map<Long, ModelClass> classMap = new HashMap<>();
		for (ModelClass cls : classes) {
			classMap.put(cls.getId(), cls);
		}

		List<Map<String, Object>> nodes = new ArrayList<>();
		List<Map<String, Object>> edges = new ArrayList<>();

		// 类节点
		for (ModelClass cls : classes) {
			Map<String, Object> node = new HashMap<>();
			node.put("id", String.valueOf(cls.getId()));
			node.put("shape", "class-node");
			Map<String, Object> data = new HashMap<>();
			data.put("classId", cls.getId());
			data.put("classIri", cls.getClassIri());
			data.put("label", StrUtil.isNotBlank(cls.getLabel()) ? cls.getLabel() : cls.getLocalName());
			data.put("classificationCode", cls.getClassificationCode());
			data.put("icon", cls.getIcon());
			data.put("color", cls.getColor());
			data.put("templateCode", cls.getTemplateCode());
			node.put("data", data);
			nodes.add(node);
		}

		// subClassOf 继承边
		List<ModelSubclassOf> subEdges = subclassOfMapper.selectList(
				Wrappers.<ModelSubclassOf>lambdaQuery().eq(ModelSubclassOf::getProjectId, projectId));
		for (ModelSubclassOf edge : subEdges) {
			Map<String, Object> e = new HashMap<>();
			e.put("source", String.valueOf(edge.getChildClassId()));
			e.put("target", String.valueOf(edge.getParentClassId()));
			e.put("shape", "subclass-edge");
			e.put("label", "subClassOf");
			e.put("data", Map.of("type", "subclass"));
			edges.add(e);
		}

		// 对象属性关联边
		List<ModelObjectProperty> objProps = objectPropertyMapper.selectList(
				Wrappers.<ModelObjectProperty>lambdaQuery().eq(ModelObjectProperty::getProjectId, projectId));
		for (ModelObjectProperty prop : objProps) {
			if (prop.getRangeClassId() == null) {
				continue; // range 未补全的跳过
			}
			Map<String, Object> e = new HashMap<>();
			e.put("source", String.valueOf(prop.getDomainClassId()));
			e.put("target", String.valueOf(prop.getRangeClassId()));
			e.put("shape", "association-edge");
			e.put("label", StrUtil.isNotBlank(prop.getLabel()) ? prop.getLabel() : prop.getLocalName());
			Map<String, Object> data = new HashMap<>();
			data.put("type", "association");
			data.put("propertyId", prop.getId());
			data.put("templateCode", prop.getTemplateCode());
			e.put("data", data);
			edges.add(e);
		}

		Map<String, Object> result = new HashMap<>();
		result.put("nodes", nodes);
		result.put("edges", edges);
		return result;
	}

	private String getCurrentUsername() {
		// 取当前登录用户名（与 createBy 审计字段一致，ontology-biz 首个主动取用户的服务，P-1 评审修正）
		// SecurityUtils.getUser().getUsername() 返回 username 字符串，存入 user_id varchar(64)
		return SecurityUtils.getUser().getUsername();
	}
}
```

> **buildGraphData**：聚合 3 张表数据返回 X6 格式的 `{nodes, edges}` JSON。节点 shape=`class-node`（自定义），继承边 shape=`subclass-edge`（实线三角箭头），关联边 shape=`association-edge`（虚线箭头带 label）。

### 4.4 Mapper + Service 接口

```java
// ModelCanvasStateMapper.java
package com.pig4cloud.pig.ontology.modeling.mapper;

import com.github.yulichang.base.MPJBaseMapper;
import com.pig4cloud.pig.ontology.modeling.entity.ModelCanvasState;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface ModelCanvasStateMapper extends MPJBaseMapper<ModelCanvasState> {
}
```

```java
// ModelCanvasService.java
package com.pig4cloud.pig.ontology.modeling.service;

import com.baomidou.mybatisplus.extension.service.IService;
import com.pig4cloud.pig.common.core.util.R;
import com.pig4cloud.pig.ontology.modeling.entity.ModelCanvasState;

import java.util.Map;

public interface ModelCanvasService extends IService<ModelCanvasState> {

	ModelCanvasState getState(Long projectId);

	R saveState(ModelCanvasState state);

	Map<String, Object> buildGraphData(Long projectId);
}
```

---

## 五、前端设计

### 5.1 API - canvas.ts

```typescript
import request from '/@/utils/request';

export function getState(projectId: string) {
	return request({
		url: '/admin/ont/model/canvas/' + projectId + '/state',
		method: 'get',
	});
}

export function saveState(projectId: string, graphData: string) {
	return request({
		url: '/admin/ont/model/canvas/' + projectId + '/state',
		method: 'put',
		data: { graphData },
	});
}

export function getGraph(projectId: string) {
	return request({
		url: '/admin/ont/model/canvas/' + projectId + '/graph',
		method: 'get',
	});
}
```

### 5.2 index.vue - 画布主页面（三栏布局）

```
┌─────────────────────────────────────────────────────────────────┐
│ [项目: 消防设备本体▼]  [布局: 自动▼]  [缩放: 100%]  [序列化预览▶]│
├──────────┬──────────────────────────────────┬───────────────────┤
│ 模板侧栏  │                                  │  属性编辑抽屉      │
│          │         AntV X6 画布              │  （双击节点展开）  │
│ ▼分类模板 │     ┌─────────┐    ┌─────────┐  │                   │
│  设备     │     │  泵     │──sa│ 喷淋泵   │  │  类名: 喷淋泵     │
│  泵       │     └─────────┘    └─────────┘  │  IRI: .../Pump    │
│  喷淋泵   │        │ contains                │                   │
│ ▼属性模板 │     ┌─────────┐                   │  数据属性(5)      │
│  name     │     │ 泵房    │                   │  - 额定流量       │
│  price    │     └─────────┘                   │  - 扬程           │
│ ▼单位     │                                  │  - 功率           │
│  m        │     [dagre 自动布局]              │  ...              │
│  CNY      │                                  │                   │
├──────────┴──────────────────────────────────┴───────────────────┤
│ 序列化预览（CodeEditor, Turtle, 可折叠）                        │
│ @prefix owl: <...> . <...FireSprayPump> a owl:Class ; ...       │
└─────────────────────────────────────────────────────────────────┘
```

**结构要点**：
- 顶部工具栏：项目选择器 + 布局切换（自动/手动）+ 缩放控制 + 序列化预览开关 + 保存画布按钮。
- splitpanes 三栏：左侧 20% 模板侧边栏 + 中间 60% X6 画布 + 右侧 20% 属性抽屉（按需展开）。
- 底部可折叠序列化预览面板（CodeEditor）。

**关键逻辑**：
```typescript
import { useCanvas } from './composables/useCanvas';
import { getState, getGraph, saveState } from '/@/api/ontology-model/canvas';
import { useMessage } from '/@/hooks/message';

const canvasRef = ref<HTMLDivElement>();
const { initGraph, renderGraph, destroyGraph, graph } = useCanvas();

const state = reactive({
	projectId: '',
	loading: false,
});

// 初始化画布
onMounted(() => {
	if (canvasRef.value) {
		initGraph(canvasRef.value);
	}
});

onUnmounted(() => {
	destroyGraph();
});

// 加载项目画布
const loadProject = async (projectId: string) => {
	state.projectId = projectId;
	state.loading = true;
	try {
		// 1. 拉图数据（类节点 + 边）
		const graphRes = await getGraph(projectId);
		renderGraph(graphRes.data.nodes, graphRes.data.edges);

		// 2. 拉画布状态（节点位置/缩放）
		const stateRes = await getState(projectId);
		if (stateRes.data?.graphData) {
			graph.fromJSON(JSON.parse(stateRes.data.graphData));
		}
	} catch (err: any) {
		useMessage().error(err.msg);
	} finally {
		state.loading = false;
	}
};

// 保存画布状态
const handleSave = async () => {
	if (!state.projectId || !graph) return;
	const graphData = JSON.stringify(graph.toJSON());
	await saveState(state.projectId, graphData);
	useMessage().success('画布状态已保存');
};
```

### 5.3 TemplateSidebar.vue - 左侧模板侧边栏

**结构**：
- `el-tabs` 三 Tab：分类模板 / 属性模板 / 单位。
- 分类模板 Tab：`el-tree` 展示分类模板树（来自 `supplyClassTemplateTree('equipment')`），节点显示"编码 + label + icon"。
- 属性模板 Tab：`el-table` 列表（来自 `/supply/v1/property-templates`），按 kind 分组。
- 单位 Tab：`el-table` 列表（来自 `/supply/v1/units`）。
- 分类模板树节点设 `draggable`，拖拽时携带 templateCode。

**拖拽处理**：
```typescript
import { supplyClassTemplateTree, supplyInherited } from '/@/api/ontology-model/class';
import { addObj as addClass } from '/@/api/ontology-model/class';
import { useMessage } from '/@/hooks/message';

// 树节点拖拽开始
const handleDragStart = (event, data) => {
	event.dataTransfer.setData('templateCode', data.templateCode);
	event.dataTransfer.setData('type', 'class-template');
};

// 画布 drop 处理（在 useCanvasDnd.ts 中）
const handleCanvasDrop = async (event) => {
	event.preventDefault();
	const templateCode = event.dataTransfer.getData('templateCode');
	if (!templateCode) return;

	// 1. 调 inherited 端点预览属性（可选，确认后创建）
	// 2. 调 addObj 创建类（含 templateCode 触发实例化）
	const localName = await promptLocalName(); // 弹窗输入类名
	const classIri = state.namespaceBase + localName;

	const res = await addClass({
		projectId: state.projectId,
		localName,
		classIri,
		templateCode, // 传 templateCode -> 后端 ClassInstantiationService 实例化属性
		label: localName,
	});

	if (res.code === 0) {
		useMessage().success('类创建成功');
		// 3. 画布添加节点
		addClassNode(res.data);
		// 4. 触发序列化预览刷新
		refreshSerializePreview();
	}
};
```

### 5.4 ClassNode.vue - X6 自定义节点组件

```vue
<template>
	<div class="class-node" :style="{ borderColor: data.color || '#409eff' }">
		<div class="class-node-header">
			<span class="class-node-icon">{{ data.icon || '📦' }}</span>
			<span class="class-node-label">{{ data.label }}</span>
		</div>
		<div class="class-node-body">
			<span class="class-node-code" v-if="data.classificationCode">
				{{ data.classificationCode }}
			</span>
			<el-tag v-if="data.templateCode" size="small" type="warning">模板</el-tag>
		</div>
	</div>
</template>

<script lang="ts" setup>
defineProps<{ data: any }>();
</script>

<style scoped>
.class-node {
	width: 160px;
	border: 2px solid #409eff;
	border-radius: 8px;
	background: #fff;
	font-size: 12px;
	overflow: hidden;
}
.class-node-header {
	padding: 6px 10px;
	background: #f5f7fa;
	font-weight: bold;
	display: flex;
	align-items: center;
	gap: 4px;
}
.class-node-body {
	padding: 4px 10px;
	display: flex;
	align-items: center;
	gap: 4px;
}
.class-node-code {
	color: #909399;
	font-size: 11px;
}
</style>
```

> **X6 自定义节点注册**：用 `@antv/x6-vue-shape` 的 `registerVueNode` 注册 `class-node` 组件（在 useCanvas.ts 初始化时注册）。

### 5.5 PropertyDrawer.vue - 右侧属性编辑抽屉

**结构**：
- `el-drawer` v-model="visible" size="400px"，title="类属性编辑 - {类名}"。
- 类信息区：classIri（readonly）/ label / labelCn / description / icon / color（可编辑，调 `class.putObj` 保存）。
- 数据属性 Tab：`el-table` 列表（来自 `class.getObj` 返回的 datatypeProperties）+ 新增/编辑/删除按钮（调 DD9 接口）。
- 对象属性 Tab：`el-table` 列表（来自 `class.getObj` 返回的 objectProperties）+ range 补全提示。

**双击节点触发**：
```typescript
// 在 useCanvas.ts 中注册事件
graph.on('node:dblclick', ({ node }) => {
	const data = node.getData();
	emitter.emit('openPropertyDrawer', data);
});

// 在 index.vue 中监听
emitter.on('openPropertyDrawer', (data) => {
	propertyDrawerRef.value.open(data.classId);
});
```

### 5.6 SerializePreviewPanel.vue - 底部序列化预览

```vue
<template>
	<div class="serialize-preview" v-show="visible">
		<div class="serialize-preview-header">
			<span>{{ t('modelCanvas.serializePreview') }}</span>
			<el-button-group>
				<el-button size="small" :type="format === 'TTL' ? 'primary' : ''" @click="format = 'TTL'">Turtle</el-button>
				<el-button size="small" :type="format === 'OWL_XML' ? 'primary' : ''" @click="format = 'OWL_XML'">OWL XML</el-button>
			</el-button-group>
			<el-button size="small" @click="visible = false">{{ t('common.collapseText') }}</el-button>
		</div>
		<code-editor v-model="content" theme="darcula" mode="turtle" readOnly height="200px" />
	</div>
</template>

<script lang="ts" setup>
import { useDebounceFn } from '@vueuse/core';
import { preview as serializePreview } from '/@/api/ontology-model/serialize';

const CodeEditor = defineAsyncComponent(() => import('/@/components/CodeEditor/index.vue'));

const props = defineProps<{ projectId: string }>();
const { t } = useI18n();

const visible = ref(true);
const content = ref('');
const format = ref('TTL');

// 防抖 500ms 触发序列化预览（AC-17.6）
const debouncedPreview = useDebounceFn(async () => {
	if (!props.projectId) return;
	const res = await serializePreview(props.projectId, format.value);
	content.value = res.data?.content || '';
}, 500);

// 监听 format 变化 + 暴露刷新方法供画布操作后调用
watch(format, debouncedPreview);

defineExpose({ refresh: debouncedPreview });
</script>
```

> **防抖 500ms**（AC-17.6）：用 `@vueuse/core` 的 `useDebounceFn`（仓库已有 `@vueuse/core 10.11.1`），画布操作（增删节点/边）后调 `previewPanelRef.refresh()` 触发防抖序列化预览。

### 5.7 composables/useCanvas.ts - 画布核心逻辑

```typescript
import { Graph } from '@antv/x6';
import { registerVueNode } from '@antv/x6-vue-shape';
import { DagreLayout } from '@antv/layout';
import ClassNode from '../components/ClassNode.vue';
import emitter from '/@/utils/mitt';

export function useCanvas() {
	let graph: Graph | null = null;

	const initGraph = (container: HTMLElement) => {
		// 注册自定义节点
		registerVueNode('class-node', ClassNode);

		graph = new Graph({
			container,
			grid: true,
			panning: true,      // 画布平移
			zooming: true,      // 画布缩放
			interacting: {
				nodeMovable: true,  // 节点可拖拽（AC-17.7 手动拖拽）
			},
			connecting: {
				allowBlank: false,
				allowLoop: false,   // 不允许自环
				allowMulti: true,   // 允许多条边（多继承）
				router: 'manhattan',
				connector: 'rounded',
			},
		});

		// 节点双击 -> 打开属性抽屉（AC-17.4）
		graph.on('node:dblclick', ({ node }) => {
			emitter.emit('openPropertyDrawer', node.getData());
		});

		// 边连接完成 -> 弹关系选择（AC-17.5）
		graph.on('edge:connected', ({ edge }) => {
			emitter.emit('edgeConnected', {
				source: edge.getSourceNode()?.getData(),
				target: edge.getTargetNode()?.getData(),
				edge,
			});
		});
	};

	const renderGraph = (nodes: any[], edges: any[]) => {
		if (!graph) return;
		graph.clearCells();
		nodes.forEach((n) => graph!.addNode(n));
		edges.forEach((e) => graph!.addEdge(e));
		// 自动布局（AC-17.7）
		autoLayout();
	};

	const autoLayout = () => {
		if (!graph) return;
		const dagre = new DagreLayout({
			type: 'dagre',
			rankdir: 'TB',     // 自上而下
			nodesep: 40,
			ranksep: 80,
		});
		const data = graph.toJSON();
		const result = dagre.layout(data);
		graph.fromJSON(result);
	};

	const destroyGraph = () => {
		if (graph) {
			graph.dispose();
			graph = null;
		}
	};

	return { initGraph, renderGraph, destroyGraph, graph: () => graph, autoLayout };
}
```

> **X6 集成要点**：
> - `registerVueNode('class-node', ClassNode)` 用 `@antv/x6-vue-shape` 注册 Vue 组件为自定义节点。
> - `Graph` 配置：grid/panning/zooming/manhattan 路由/rounded 连接器。
> - 事件：`node:dblclick`（AC-17.4 双击编辑）、`edge:connected`（AC-17.5 边创建）。
> - 自动布局：`@antv/layout` 的 DagreLayout（dagre 算法，TB 自上而下）。

### 5.8 composables/useCanvasDnd.ts - 拖拽创建逻辑

```typescript
import emitter from '/@/utils/mitt';
import { addObj as addClass } from '/@/api/ontology-model/class';
import { addEdge as addSubclassEdge } from '/@/api/ontology-model/subclassof';
import { addObj as addObjectProp } from '/@/api/ontology-model/object-property';
import { useMessage } from '/@/hooks/message';

export function useCanvasDnd(projectId: Ref<string>, namespaceBase: Ref<string>) {
	// 拖拽分类模板 -> 创建类（AC-17.3）
	const handleDrop = async (event: DragEvent) => {
		event.preventDefault();
		const templateCode = event.dataTransfer?.getData('templateCode');
		if (!templateCode || !projectId.value) return;

		const localName = window.prompt('请输入类名（如 FireSprayPump）');
		if (!localName) return;

		try {
			const res = await addClass({
				projectId: projectId.value,
				localName,
				classIri: namespaceBase.value + localName,
				templateCode,
				label: localName,
			});
			if (res.code === 0) {
				useMessage().success('类创建成功');
				emitter.emit('canvasNodeAdded', res.data);
			}
		} catch (err: any) {
			useMessage().error(err.msg);
		}
	};

	// 边连接完成 -> 建关系（AC-17.5）
	const handleEdgeConnected = async ({ source, target, edge }) => {
		// 弹窗选择关系类型：subClassOf / 对象属性
		const relationType = await promptRelationType();
		if (!relationType) {
			edge.remove(); // 取消则删除边
			return;
		}

		try {
			if (relationType === 'subclass') {
				await addSubclassEdge({
					projectId: projectId.value,
					childClassId: source.classId,
					parentClassId: target.classId,
				});
				useMessage().success('类层级建立成功');
			} else if (relationType === 'association') {
				const propName = window.prompt('请输入属性名（如 contains）');
				if (!propName) {
					edge.remove();
					return;
				}
				await addObjectProp({
					projectId: projectId.value,
					domainClassId: source.classId,
					rangeClassId: target.classId,
					localName: propName,
					propertyIri: source.label + '_' + propName,
					label: propName,
				});
				useMessage().success('对象属性建立成功');
			}
			emitter.emit('canvasEdgeAdded');
		} catch (err: any) {
			edge.remove(); // 失败则删除边
			useMessage().error(err.msg);
		}
	};

	return { handleDrop, handleEdgeConnected };
}
```

### 5.9 i18n（zh-cn.ts 摘要）

```typescript
export default {
	modelCanvas: {
		selectProject: '请选择项目',
		layout: '布局',
		autoLayout: '自动布局',
		manualLayout: '手动布局',
		zoom: '缩放',
		serializePreview: '序列化预览',
		saveCanvas: '保存画布',
		// 模板侧边栏
		templateSidebar: '模板',
		classTemplate: '分类模板',
		propertyTemplate: '属性模板',
		unit: '单位',
		// 节点
		classNode: '类节点',
		propertyCount: '属性数',
		templateBadge: '模板',
		// 边
		subClassOf: '继承',
		association: '关联',
		selectRelationType: '请选择关系类型',
		// 属性抽屉
		propertyDrawer: '属性编辑',
		classInfo: '类信息',
		datatypeProperties: '数据属性',
		objectProperties: '对象属性',
		// 提示
		inputClassName: '请输入类名（如 FireSprayPump）',
		inputPropertyName: '请输入属性名（如 contains）',
		saveSuccess: '画布状态已保存',
		createSuccess: '创建成功',
		// 拖拽
		dragTemplateTip: '拖拽分类模板到画布创建类',
	},
};
```

---

## 六、横切设计

### 6.1 缓存

| 缓存 | 实现 | 说明 |
|---|---|---|
| 画布图数据 | 后端 buildGraphData 实时查表 | 不缓存，每次加载画布实时查 3 张表 |
| 画布状态 | ont_model_canvas_state 持久化 | graph.toJSON() 存后端，每用户独立 |
| 序列化预览 | DD10 Caffeine 缓存（5min TTL） | 画布操作后调 invalidateCache 失效 |

### 6.2 校验

| 校验点 | 实现位置 | 说明 |
|---|---|---|
| 项目选择必填 | index.vue | 未选项目时画布空白，禁用拖拽 |
| 拖拽类名非空 | useCanvasDnd.handleDrop | prompt 输入为空则取消 |
| 边自环禁止 | X6 Graph connecting.allowLoop=false | AC-17.5 不允许自环 |
| 环路检测 | DD9 SubclassOfServiceImpl.wouldCreateCycle | subClassOf 建立时后端校验 |
| 对象属性 range 必填 | DD9 ObjServiceImpl.saveProp | 画布建关联边时 range 由目标节点提供 |

### 6.3 事件通信（mitt）

| 事件 | 发送方 | 接收方 | 说明 |
|---|---|---|---|
| `canvasNodeAdded` | useCanvasDnd | index.vue | 新增节点后刷新画布 + 序列化预览 |
| `canvasEdgeAdded` | useCanvasDnd | index.vue | 新增边后刷新序列化预览 |
| `openPropertyDrawer` | useCanvas | index.vue | 双击节点打开属性抽屉 |
| `edgeConnected` | useCanvas | useCanvasDnd | 边连接完成触发关系创建 |
| `refreshSerializePreview` | index.vue | SerializePreviewPanel | 画布操作后触发防抖预览 |

> **mitt 事件总线**：复用 `web/src/utils/mitt.ts`（已就绪），在 `types/mitt.d.ts` 扩展画布事件键。需新增的 MittType 键（P-4 评审补充）：
> ```typescript
> // 在 mitt.d.ts 的 MittType 中新增：
> canvasNodeAdded?: any;           // 新增节点后刷新画布 + 序列化预览
> canvasEdgeAdded?: any;           // 新增边后刷新序列化预览
> openPropertyDrawer?: any;        // 双击节点打开属性抽屉
> edgeConnected?: any;             // 边连接完成触发关系创建
> refreshSerializePreview?: any;   // 画布操作后触发防抖预览
> ```

### 6.4 权限

| 操作 | 权限点 | 说明 |
|---|---|---|
| 画布加载/查看 | `ont_canvas_view` | 菜单访问 |
| 画布状态保存 | `ont_canvas_manage` | 保存按钮 v-auth |
| 拖拽建类 | `ont_class_model_manage` | 复用 DD8 权限 |
| 属性编辑 | `ont_prop_model_manage` | 复用 DD9 权限 |
| 建关系 | `ont_class_model_manage` / `ont_prop_model_manage` | subClassOf / 对象属性 |
| 序列化预览 | `ont_serialize_view` | 复用 DD10 权限 |

> **前端直调 Supply API 鉴权**（P-5 评审补充）：前端 request 拦截器（`web/src/utils/request.ts`）自动为所有请求附加 Authorization token，因此前端直调 `/admin/ont/supply/v1/` 无鉴权问题（与后端 RestTemplate 调后端的鉴权难题不同）。Supply 端点的 `@HasPermission("ont_supply_view")` 由前端携带的 token 鉴权通过。

### 6.4 权限

| 操作 | 权限点 | 说明 |
|---|---|---|
| 画布加载/查看 | `ont_canvas_view` | 菜单访问 |
| 画布状态保存 | `ont_canvas_manage` | 保存按钮 v-auth |
| 拖拽建类 | `ont_class_model_manage` | 复用 DD8 权限 |
| 属性编辑 | `ont_prop_model_manage` | 复用 DD9 权限 |
| 建关系 | `ont_class_model_manage` / `ont_prop_model_manage` | subClassOf / 对象属性 |
| 序列化预览 | `ont_serialize_view` | 复用 DD10 权限 |

### 6.5 性能（NFR-P3）

| 指标 | 目标 | 实现 |
|---|---|---|
| 画布首屏渲染（≤100 节点） | <1s | buildGraphData 查 3 表 + X6 fromJSON 渲染 |
| 画布操作流畅度 | 60fps | X6 Canvas 渲染引擎；dagre 布局异步 |
| 序列化预览防抖 | 500ms | useDebounceFn 避免频繁请求 |

### 6.6 加密注意

pig-ui `request.ts` 默认对请求体加密（`wrapEncryption`）。画布状态保存（`graph.toJSON()` 可能产生大 JSON）时需评估加密性能。若 JSON 过大，可加 `headers: { 'Enc-Flag': false }` 关闭加密。

---

## 七、测试要点

| 类别 | 要点 |
|---|---|
| 编译 | 后端 modeling 包新增类无编译错误；前端 `npm install @antv/x6 @antv/x6-vue-shape @antv/layout` 成功 |
| Flyway（V16） | `SELECT count(*) FROM ont_model_canvas_state` = 0；`SELECT menu_id FROM sys_menu WHERE menu_id BETWEEN 11500 AND 11502` = 3 条 |
| 覆盖（AC-17.1） | `GET /{projectId}/graph` 返回 `{nodes, edges}` JSON；画布渲染类节点（含 label/icon/classificationCode）+ 继承边（实线三角箭头）+ 关联边（虚线带 label） |
| 覆盖（AC-17.2） | 左侧侧边栏三 Tab 加载分类模板树（来自 Supply class-template/tree）+ 属性模板列表（Supply property-templates）+ 单位列表（Supply units） |
| 覆盖（AC-17.3） | 拖拽分类模板"喷淋泵"到画布 -> 弹窗输入类名 -> 调 `POST /admin/ont/model/class`（含 templateCode）-> 画布新增类节点 + 属性自动实例化 |
| 覆盖（AC-17.4） | 双击类节点 -> 右侧抽屉展开 -> 展示类信息 + 数据属性列表 + 对象属性列表；编辑 label 后调 `PUT /admin/ont/model/class/{id}` 保存 |
| 覆盖（AC-17.5） | 从类节点端口拖出 -> 连到目标节点 -> 弹关系选择（subClassOf/对象属性）-> 选 subClassOf 调 `POST /admin/ont/model/subclassof`；选对象属性输入属性名调 `POST /admin/ont/model/object-property` |
| 覆盖（AC-17.6） | 画布操作后 500ms 内底部序列化预览刷新（CodeEditor 展示 Turtle）；连续操作只触发一次（防抖） |
| 覆盖（AC-17.7） | 点击"自动布局"-> dagre 重排节点（TB 方向）；手动拖拽节点位置可调 |
| 覆盖（AC-17.8） | 拖拽节点后点击"保存画布"-> `PUT /{projectId}/state` 持久化 graph.toJSON()；刷新页面后 `GET /{projectId}/state` 恢复上次布局 |
| 边自环禁止 | 尝试从节点连到自身，X6 拒绝（allowLoop=false） |
| 环路检测 | 画布建 subClassOf(A,B) + subClassOf(B,A)，后者后端返回 `R.failed("建立此关系会导致类层级成环")`，画布边自动删除 |
| 对象属性 range | 画布建关联边时 range 由目标节点提供（非空），后端 saveProp 校验通过 |
| 多继承 | 从 A 拖出两条继承边分别连 B 和 C，两条均成功（allowMulti=true） |
| 性能 | 100 节点画布首屏渲染 <1s；操作流畅 60fps |
| 权限 | 无 `ont_canvas_view` 调 getGraph/getState 返回 403；无 `ont_canvas_manage` 调 saveState 返回 403 |
| 序列化预览切换 | 预览面板切换 TTL/OWL_XML 格式，CodeEditor 内容更新 |

---

## 八、风险与缓解

| 风险 | 缓解 |
|---|---|
| AntV X6 与 Vite 8 / TS 4.9 兼容性 | M9 放最后；先验证 `npm install` + 最小 Graph 渲染；X6 2.x 官方支持 Vite 5+，Vite 8 兼容性需实测 |
| X6 学习曲线 + 集成成本（R-17） | DD8-DD10 表单建模已能完成全部操作，画布是增值层；延期不影响主链路 |
| graph.toJSON() JSON 过大 | ont_model_canvas_state.graph_data 用 text 类型无硬限；>100 节点提示拆分项目（R-22） |
| 画布状态加密性能 | request.ts 默认加密大 JSON 可能慢；必要时 `headers:{'Enc-Flag':false}` 关闭 |
| dagre 布局大图性能 | 100 节点 dagre 布局 <1s；超大规模后续改增量布局 |
| 多用户画布状态覆盖 | uk(project_id, user_id) 每用户独立（R-25）；v1 不做协同编辑 |
| CodeEditor 缺 turtle mode | 复用 DD10 补充的 `codemirror/mode/turtle/turtle` 引入 |

---

## 九、交付物清单

| 类型 | 文件 | 说明 |
|---|---|---|
| 新建 | `modeling/entity/ModelCanvasState.java` | 画布状态 Entity |
| 新建 | `modeling/mapper/ModelCanvasStateMapper.java` | 画布状态 Mapper |
| 新建 | `modeling/service/ModelCanvasService.java` | 画布 Service 接口 |
| 新建 | `modeling/service/impl/ModelCanvasServiceImpl.java` | 画布 Service 实现（含 buildGraphData） |
| 新建 | `modeling/controller/ModelCanvasController.java` | 画布 Controller |
| 新建 | `V16__ont_model_canvas_seed.sql` | 建表 + 菜单种子（11500 段） |
| 新建 | `api/ontology-model/canvas.ts` | 前端 API |
| 新建 | `views/admin/ontology-model/canvas/index.vue` | 画布主页面 |
| 新建 | `views/admin/ontology-model/canvas/components/TemplateSidebar.vue` | 左侧模板侧边栏 |
| 新建 | `views/admin/ontology-model/canvas/components/ClassNode.vue` | X6 自定义节点组件 |
| 新建 | `views/admin/ontology-model/canvas/components/PropertyDrawer.vue` | 右侧属性编辑抽屉 |
| 新建 | `views/admin/ontology-model/canvas/components/SerializePreviewPanel.vue` | 底部序列化预览 |
| 新建 | `views/admin/ontology-model/canvas/composables/useCanvas.ts` | 画布核心逻辑 |
| 新建 | `views/admin/ontology-model/canvas/composables/useCanvasDnd.ts` | 拖拽创建逻辑 |
| 新建 | `views/admin/ontology-model/canvas/i18n/zh-cn.ts` | 中文词条 |
| 新建 | `views/admin/ontology-model/canvas/i18n/en.ts` | 英文词条 |
| 修改 | `web/package.json` | 新增 @antv/x6 + @antv/x6-vue-shape + @antv/layout |
| 修改 | `web/src/types/mitt.d.ts` | 扩展画布事件键（canvasNodeAdded/canvasEdgeAdded/openPropertyDrawer/edgeConnected/refreshSerializePreview，P-4） |

---

## 十、与 PRD 边界的对齐确认（防混淆备忘）

| 维度 | PRD 约定 | 本 DD 实现 | 对齐 |
|---|---|---|---|
| 画布引擎 | AntV X6 2.x（PRD FR-17 / 3.3） | @antv/x6 + @antv/x6-vue-shape + @antv/layout | ✓ |
| 类节点+继承边+关联边 | subClassOf 实线三角箭头，对象属性虚线带 label（PRD FR-17.1） | class-node 自定义节点 + subclass-edge + association-edge | ✓ |
| 模板侧边栏三 Tab | 分类模板树/属性模板/单位（PRD FR-17.2） | TemplateSidebar.vue 调 Supply 3 端点 | ✓ |
| 拖拽创建实例化 | 拖分类模板->调 inherited->实例化（PRD FR-17.3） | useCanvasDnd.handleDrop -> addClass(templateCode) | ✓ |
| 双击编辑 | 双击节点->属性抽屉（PRD FR-17.4） | node:dblclick -> PropertyDrawer | ✓ |
| 边创建选关系 | 端口拖出->选 subClassOf/对象属性（PRD FR-17.5） | edge:connected -> promptRelationType | ✓ |
| 序列化预览防抖 500ms | 画布操作->防抖->预览（PRD FR-17.6） | useDebounceFn(500ms) + CodeEditor | ✓ |
| 自动布局+手动拖拽 | dagre 自动 + 手动拖拽（PRD FR-17.7） | DagreLayout(TB) + panning/nodeMovable | ✓ |
| 画布状态持久化 | graph.toJSON() 存后端，每用户独立（PRD FR-17.8） | ont_model_canvas_state uk(project_id,user_id) | ✓ |
| 菜单 ID | 11500 段（PRD 13.1） | 11500 菜单 + 11501~11502 按钮 | ✓ |
| 权限标识 | ont_canvas_view / ont_canvas_manage（PRD 13.2） | Controller @HasPermission 对齐 | ✓ |
| Flyway | V16（PRD 9 / 设计计划 3.2） | V16__ont_model_canvas_seed.sql | ✓ |
| 画布是增值层 | M6-M8 表单能完成全部建模，画布延期不影响主链路（PRD M9 建议） | DD11 放最后，风险最高（R-17） | ✓ |
| 治理域只读消费 | 不修改治理域表/接口（PRD NFR-C3/R-23） | 只调 Supply API，不写治理域表 | ✓ |
| 多用户独立状态 | 每用户独立画布状态（PRD R-25） | uk(project_id, user_id) | ✓ |
| 不做协同编辑 | v1 每用户独立，不做 OT/CRDT（PRD 第十八节） | 每用户独立状态，无协同 | ✓ |

---

*本 DD 是建模域最后一份详细设计，完成可视化建模画布能力。评审通过后进入实现，实现完成后更新设计计划 DD11 状态为"已完成"，建模域 M5-M9 全部详细设计收尾。后续 M10（个体实例 owl:NamedIndividual）为独立里程碑，不在 v1 范围。*
