# 本体模板化治理功能 详细设计计划

| 项 | 内容 |
|---|---|
| 文档名称 | 本体模板化治理功能 详细设计计划 |
| 文档版本 | v1.0 |
| 上游 PRD | 《本体模板化治理功能产品需求文档.md》v1.2（评审通过） |
| 所属产品 | youming（pig + pig-ui 二次开发） |
| 编写日期 | 2026-07-25 |
| 文档状态 | 进行中（M0 详细设计已出，M1-M4 + 贯穿项待逐个详细设计） |

---

## 一、计划总览

本计划把 PRD 的 6 个里程碑（M0 模块骨架 / M1 属性模板+供给 / M2 分类模板+编码+类树 / M3 单位注册表 / M4 注释属性注册表 / 贯穿：参考本体）拆成 **6 个详细设计单元**，每单元产出一份独立详细设计文档，存放于 `docs/ontology/模板化建模平台设计/prd/` 目录，命名 `详细设计-DDx-<主题>.md`。

```
DD1 (M0) 模块骨架与基础设施   ← 本次已完成
DD2 (M1) 属性模板库 + 供给接口
DD3 (M2) 分类模板体系 + 编码 + 类层级镜像
DD4 (M3) 单位注册表（QUDT 引用）
DD5 (M4) 注释属性注册表
DD6 (贯穿) 参考本体库浏览与导入
```

每个详细设计单元遵循统一结构：
1. 设计目标与范围（对应 PRD 的 FR 与里程碑）
2. 落地清单（文件清单：pom/类/配置/sql/vue/ts）
3. 数据库设计（DDL + 种子，含字段类型与约束）
4. 后端设计（包结构、Entity/Service/Controller、接口签名、鉴权）
5. 前端设计（页面、API、路由挂载、组件复用）
6. 横切设计（缓存/校验/异常/审计/i18n）
7. 验收映射（AC 编号 -> 实现点）
8. 测试要点

---

## 二、各单元详细设计范围

### DD1（M0）模块骨架与基础设施 ✅ 已完成

- **范围**：搭建 `pig-ontology-biz` 单模块骨架（S-4，不拆 api/biz）；`server/pom.xml` 注册；pig-boot 引依赖；pig-gateway 路由；V4 治理表结构 + V5 种子（sys_menu 10000+ 段 + 权限点 + 内置模板占位）。
- **输入**：PRD 3.1/3.2/3.3、NFR-10/11、13.1/13.2、十六节 M0。
- **产出**：本文档 + `详细设计-DD1-模块骨架与基础设施.md`。

### DD2（M1）属性模板库 + 供给接口

- **范围**：FR-1（属性模板 CRUD + 内置 12 模板种子）+ FR-5 供给接口 v1（属性模板部分）+ FR-6 提升/弃用。
- **数据**：`ont_property_template`（9.1）。
- **后端**：PropertyTemplateController/Service/ServiceImpl/Mapper + SupplyController（属性模板供给）+ PromoteService。
- **前端**：`views/ontology/property-template/index.vue`（列表+表单）+ `api/ontology/property-template.ts`。
- **AC**：AC-1.1~1.8 + AC-5.1~5.7 + AC-6.1~6.6。

### DD3（M2）分类模板体系 + 编码 + 类层级镜像

- **范围**：FR-2（分类模板树+继承+覆盖）+ FR-8（编码规范）+ FR-9（类层级供给建议+只读镜像）。
- **数据**：`ont_class_template`（9.2）、`ont_class_template_ref`（9.3）、`ont_classification_rule`（9.7）、`ont_class_hierarchy`（9.8 只读镜像）。
- **后端**：ClassTemplateController + ClassificationRuleController + ClassHierarchyController（只读）+ SupplyController（继承视图/suggest）+ SyncController（建模侧同步，10.7）；继承合并算法 + 环路 DFS + 编码生成器 + Caffeine 缓存。
- **前端**：`views/ontology/class-template/index.vue`（左树右表，复用 QueryTree+splitpanes）+ `views/ontology/class-hierarchy/index.vue`（只读类树）+ 编码规则配置抽屉。
- **AC**：AC-2.1~2.8 + AC-8.1~8.7 + AC-9.1~9.6。

### DD4（M3）单位注册表（QUDT 引用）

- **范围**：FR-3（量纲+单位+换算）+ FR-5 供给扩展（单位部分）。
- **数据**：`ont_quantity_kind`（9.4）、`ont_unit`（9.5）、`ont_governance_meta`（版本快照）。
- **后端**：QuantityKindController + UnitController + SupplyController（单位/换算）+ 换算引擎（BigDecimal + conversionMultiplierSN）。
- **前端**：`views/ontology/unit/index.vue`（左量纲树右单位列表 + 换算试算区，前端本地算系数）。
- **AC**：AC-3.1~3.8。

### DD5（M4）注释属性注册表

- **范围**：FR-4（注释属性注册表 CRUD + 导出）+ FR-5 供给扩展（注释属性部分）。
- **数据**：`ont_annotation_property`（9.6）。
- **后端**：AnnotationPropertyController + SupplyController（注释属性供给）+ 导出（Markdown/Excel）。
- **前端**：`views/ontology/annotation-property/index.vue`（列表 + appliesTo 过滤 + 导出）。
- **AC**：AC-4.1~4.5。

### DD6（贯穿）参考本体库浏览与导入

- **范围**：FR-7（QUDT/Brick/CCO 只读浏览 + 导入到 ont_unit/ont_class_template）。
- **后端**：ReferenceController + TTL 解析器（Apache Jena 或轻量正则）+ 解析缓存 + 导入映射器（Brick subClassOf -> parent_id）。
- **前端**：`views/ontology/reference/index.vue`（三 Tab + 分页 + 导入按钮）。
- **AC**：AC-7.1~7.5。

---

## 三、设计原则与通用约定（所有 DD 共享）

### 3.1 后端约定

- **模块**：仅 `pig-ontology-biz`（S-4），包根 `com.pig4cloud.pig.ontology`。
- **启动类**：`com.pig4cloud.pig.ontology.PigOntologyApplication`，注解 `@EnableOpenApi("ontology")` + `@EnablePigResourceServer` + `@EnableDiscoveryClient` + `@SpringBootApplication`（对齐 PigAdminApplication）。单体模式下不启用，由 PigBootApplication 聚合。
- **分层**：`controller` / `service`(+`impl`) / `mapper` / `api/entity` / `api/dto` / `api/vo`。
- **Entity 范式**：继承 `Model<T>` + `@TableId(type=IdType.ASSIGN_ID)` + `@TableName`（治理表名与类名驼峰推断不一致，**需显式标注**）+ `@TableLogic` 标 `delFlag`（String "0"/"1"）+ `@TableField(fill=...)` 标审计字段。
- **返回与鉴权**：Controller 用 `R<T>` 统一返回；`@HasPermission("ont_xxx")` 鉴权；写操作加 `@SysLog("...")`。
- **供给接口版本化**：路径 `/admin/ont/supply/v1/**`，返回结构以 VO 稳定化，表结构变更不破坏 VO。

### 3.2 数据库约定

- **Flyway 脚本**：放 `pig-common-data` 的共享 `db/migration/`，治理用 V4（结构）+ V5（种子）+ V6+（后续里程碑增量）。**禁改已应用脚本**。
- **字段类型**：主键 `bigint NOT NULL`；布尔 `char(1) DEFAULT '0'/'1'`（不用 smallint/boolean）；时间 `timestamp DEFAULT now()`；审计 `create_by varchar(64)`/`update_by varchar(64)`；逻辑删除 `del_flag char(1) DEFAULT '0'`。
- **表前缀**：`ont_`。
- **注释**：每表每字段加 `COMMENT ON`。

### 3.3 前端约定

- **页面**：`web/src/views/ontology/<feature>/index.vue`，路径须与 sys_menu.path 严格对应（S-6）。
- **API**：`web/src/api/ontology/<feature>.ts`，用 `/@/utils/request`，函数名 `pageList/addObj/getObj/putObj/delObj`。
- **路由**：后端控路由，不写 route.ts；靠 sys_menu 种子 + `import.meta.glob` 动态装配。
- **树组件**：分类模板树/类树复用 `components/QueryTree` + `splitpanes` + `utils/other.ts:handleTree`。
- **i18n**：词条放 `views/ontology/<feature>/i18n/*.ts`，en/zh-cn 双语。

### 3.4 横切约定

- **缓存**：继承视图、单位换算系数用 Caffeine 本地缓存（key+TTL，变更失效）。
- **异常**：业务错误用 `R.failed(msg)`（非 500）；唯一约束冲突捕获并转友好提示。
- **审计**：MybatisPlusMetaObjectHandler 自动填充审计字段，无需手写。

---

## 四、交付顺序与依赖

```
DD1 (M0) ──┬─> DD2 (M1) ──> DD3 (M2)
           │
           └─> DD4 (M3) ──────────────────┐
                                           ├─> DD6 (贯穿, 依赖 M2+M3)
           DD2 (M1) ──> DD5 (M4) ──────────┘
```

- DD1 先行（骨架），是所有后续 DD 的基础。
- DD2 是首落地（属性模板 ROI 最高），供给接口 v1 随 DD2 起步、逐 DD 扩展。
- DD3 依赖 DD2（分类模板结构骨架引用属性模板）。
- DD4 可与 DD2 并行（只引用 QUDT，与 M1 仅在"模板预设单位"交汇）。
- DD5 依赖 DD2（templateRef 需注册）。
- DD6 贯穿，依赖 DD3（Brick 导入分类模板）+ DD4（QUDT 导入单位）。

---

*本计划随各 DD 详细设计的完成逐单元更新状态。M0 详细设计见同目录 `详细设计-DD1-模块骨架与基础设施.md`。*
