# 01 · 可行性分析报告 评审

> 评审对象：pig 原始设计《01-MySQL迁移PostgreSQL可行性分析报告》
> 评审基线：本仓库 `server/`（pig 二次开发）`youming` 分支源码

## 一、总体评价

**通过。** 报告结构完整（结论摘要→项目盘点→库结构盘点→代码层影响→策略→总评），论断经源码核验属实，可行性结论可信。报告作为决策依据可直接使用。

## 二、源码核验（逐条）

报告"核心判断依据"5 条 + "风险点"7 条，核验如下：

### 核心判断依据核验

| # | 报告论断 | 核验 | 结果 |
|---|---|---|---|
| 1 | 手写 SQL 全 ANSI 兼容、无 MySQL 专有函数 | grep 18 个 Mapper XML 的 `IF/FIND_IN_SET/GROUP_CONCAT/DATE_FORMAT/IFNULL/NOW()/SUBSTRING_INDEX/${}` | ✅ 0 命中 |
| 2 | 仅 `gen_field_type` 用 AUTO_INCREMENT，余皆 ASSIGN_ID 雪花 | `grep -c AUTO_INCREMENT db/pig.sql`=1；entity 多处 `IdType.ASSIGN_ID` | ✅ |
| 3 | DsTypeEnum.PG 已存在、anyline BOM 已管 PG 适配器、gen_field_type 预置 PG 类型 | 读 DsTypeEnum L30-31、BOM L235、gen_field_type 含 int8/timestamptz 等 | ✅ |
| 4 | MetaObjectHandler 兜底审计字段，ON UPDATE 可移除 | 读 MybatisPlusMetaObjectHandler L26-43 | ✅ |
| 5 | 无存储过程/触发器/函数 | pig.sql 为纯 DDL+INSERT | ✅ |

### 风险点核验

| 风险 | 报告等级 | 核验 | 评审意见 |
|---|---|---|---|
| Quartz 11 张表 | 中 | pig.sql 含 `qrtz_*` 11 表、含 blob/FK | 等级合理，整体替换官方脚本是正解 |
| Nacos pig_config | 中 | application.properties L6 `platform=mysql` | ~~建议上调为"中高"~~ **2026-07-08 更新：风险消除**，见总报告 N3：youming 用的 Nacos 3.2.2 原生内置 PG 支持（`nacos-datasource-plugin-postgresql-3.2.2.jar` 含 pg-schema.sql + PostgresqlDatabaseDialect） |
| codegen anyline 适配器未引入 | 低 | codegen pom L97 仅 mysql 适配器 | 等级合理，加一行依赖即可 |
| gen_template MySQL 语法 | 中 | 模板存 DB，未在 pig.sql 中（需导出审计） | 等级合理，属应用层非库结构 |
| GenDynamicMapper 裸 SQL | 低 | 待运行时确认 | 报告已标"需确认"，合规 |
| 字符串转义 | 中 | sys_oauth_client_details、gen_template 含 `\'`/`\"` | 等级合理，standard_conforming_strings=on 下需改 |
| 标识符大小写 | 低 | 表名本为小写 | 等级合理，但 `name/type/sort` 等列名加双引号的建议要落到转换脚本里 |

## 三、应修订项

### S1（跨文档，详见 02 评审）—— 不在此重复

### 01 自身：风险表 Nacos 一行建议补一句 PoC 要求

报告第四节 4.5 与风险表对 Nacos PG 仅写"需确认所用 Nacos 版本的 PG 支持成熟度"。建议补一句可操作的验收指向：

> 建议在 P2 设 Nacos PG PoC 门禁（启动 / 8 条种子导入 / 动态刷新 / 历史版本查询四项验收），详见《02-迁移详细设计》评审 N3。

不修改不影响通过，但能让"中"风险从"知道有风险"升级为"有可验收的关停条件"。

## 四、亮点

1. **结论先行**：报告一开头给"可行性：高"+ 5 条依据，决策者无需通读即可判断，符合可行性报告写作规范。
2. **量化盘点**：库结构专有特性统计表（ENGINE 49 / CHARACTER SET 308 / 反引号 1338 / datetime 94 / COMMENT 520...）让"机械转换工作量"可估算，非泛泛而谈。
3. **多 DB 架构就绪度判断准确**：识别出 anyline 抽象层 + DsTypeEnum.PG + gen_field_type 类型映射是"代码层已具备、仅依赖未引入"的状态，这是该迁移"低代码改动"的关键洞察，报告抓住了。

## 五、结论

报告通过，作为迁移可行性决策依据使用。~~唯一建议是 Nacos 风险从"中"实质对待为"需 PoC 门禁的中高"~~。**2026-07-08 复核**：Nacos 3.2.2 原生支持 PG，此风险消除，无需上调等级。其余论断扎实，无需改动。
